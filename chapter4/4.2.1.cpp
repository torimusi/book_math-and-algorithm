#include <iostream>
using namespace std;

int N, M, A[10000009], B[200009];

int main() {
    cin >> N;
    for (int i = 1; i <= N - 1; i++) cin >> A[i];
    cin >> M;
    for (int i = 1; i <= M; i++) cin >> B[i];

    // 階差で考える
    // 初項 と 一つ前の項との関係
    
    return 0;
}