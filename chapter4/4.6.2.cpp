#include <iostream>
using namespace std;

int X, Y;
int Divider = 1000000007;

int main() {
    cin X >> Y;

    // ナイトは(0, 0)からスタート
    // 移動は(i + 1, j + 1) or (i + 2, j + 1)のみ
    // 場合の数をまず求める
    // 計算途中で1000000007で割ってオーバーフローを防ぐ

    return 0;
}