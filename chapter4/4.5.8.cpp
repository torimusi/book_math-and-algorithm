#include <bits/stdc++.h>
using namespace std;

int K, dist[1 << 18];
bool used[1 << 18];
vector<pair<int, int>> G[1 << 18];
priority_queue<pair<int, int>, vector<pair<int. int>>, greater<pair<int, int>>> Q;

// ダイクストラ法
void dijkstra() {
    
}

int main() {
    int K;
    cin >> K;

    // 各桁の和の最小値は K 以下であるはず
    for (int i = 1; i <= K; i++) {

    }
}