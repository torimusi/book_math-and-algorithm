#include <iostream>
#include <algoruthm>
using namespace std;

long long N;
long long A[100009];

int GCD (int a, int b){
    if (b >= a) {
        int c = a;
        a = b;
        b = c;
    }
    while (){
        a - b;
    }
}

int main() {
    cin >> N;
    for (int i = 1, i <= N; i++) cin >> A[i];
    
    GCD();

    cout << endl;
    return 0;
}